VITE_API_URL=http://localhost:8000
VITE_APP_NAME=Hub Client Portal
VITE_REFERRAL_BASE_URL=https://app.rbacplatform.com/register?ref=
# Subdomain used to fetch public branding (logo, favicon, org name) on the landing page.
# Set to the subdomain of your main tenant (e.g. "system"). Required in production.
VITE_TENANT_SUBDOMAIN=
